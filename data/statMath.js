

function findPercentile() {


  `Calculate individual stats`;
  const stat1 = printf("Stat 1: %d\n", stat1);
  const stat2 = printf("Stat 2: %d\n", stat2);
  const stat3 = printf("Stat 3: %d\n", stat3);

  `Calculate stat total`;
  const totalStat = stat1 + stat2 + stat3;
  printf("Total Stat: %d\n", totalStat);

  `Calculate stat percentage`;
  const statPercentage = (totalStat / 45) * 100;
  printf("Stat Percentage: %.2f%%\n", statPercentage);


}
